# Graveblood reconstruction build products
reconstruction/build/
reconstruction/*.gba
reconstruction/*.elf
reconstruction/*.map
reconstruction/*.sav
reconstruction/*.sym
reconstruction/*.d
reconstruction/*.o

# Python cache files must never enter packaged RE workspaces
__pycache__/
*.pyc
